node_modules/
.env
.env.local
*.log
.DS_Store

.vercel

# Exports de chats de WhatsApp: datos personales de terceros. Van fuera del
# repo (ver scripts/group-mining/README.md); esto es la red de seguridad por
# si alguien los deja adentro.
scripts/group-mining/exports/
reporte-grupos.html

# Estado local del agente y artefactos de herramientas
.claude/
.playwright-mcp/
__pycache__/
*.pyc
dbg.log

.vercel

# Subagent-driven-development scratch state (progress ledger)
.superpowers/

# EPE: dependencias y bundle generado
epe/node_modules/
epe/dist/

# Datos personales de terceros. Mismo motivo que los exports de arriba: son
# nombres, telefonos y jids de colegas que no consintieron nada. Un commit es
# permanente; una vez adentro no se saca sin reescribir la historia.
recuperacion-grupos/

# Cuentas de cobro y soportes con datos fiscales reales. Un repo de codigo no
# es el lugar de la contabilidad.
facturacion/

# Assets de campanas (PDFs, imagenes, paginas guardadas). Material de
# referencia, no codigo — ver CLAUDE.md. Son ~38 MB que la historia de git
# cargaria para siempre en cada clone.
CAMPAÑAS/
DIAMOND/

# Artefactos temporales de trabajo (renders, builds de carrusel, pruebas).
scratch/
